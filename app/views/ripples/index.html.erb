<p id="notice"><%= notice %></p>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Message</th>
      <th>Posted</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @current_ripples.each do |ripple| %>
      <tr>
	<% if(ripple.url.nil? || ripple.url.empty?) %>
            <td><%= ripple.name %></td>
	<% else %>
	    <td><%= link_to ripple.name, "http://#{ripple.url}" %></td>
	<% end %>
        <td><%= ripple.message[0..50].gsub(/\s\w+$/,'...') %></td>
	<td><%= ripple.created_at %></td>
        <td><%= link_to 'Show', ripple %></td>    
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= @cr %>
<%= link_to 'Newest', ripples_path(:page_number => 1) %> |
<%= link_to 'Previous 10 Ripples', ripples_path(:page_number => 2) %> |
<%= link_to 'Next 10 Ripples', ripples_path(:page_number => 3) %> |
<%= link_to 'Oldest', ripples_path(:page_number => 4) %> |
<%= link_to 'New Ripple', new_ripple_path %>
